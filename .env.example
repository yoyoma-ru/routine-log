# Neon Postgres の接続文字列をここに設定する（実体は .env にコピーして使う。.env はコミットしない）
# Neon のダッシュボード → Connection string をコピーし、先頭を postgresql+psycopg2:// に書き換える。
# 例:
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@ep-xxxx.ap-southeast-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=
